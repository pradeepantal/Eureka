spring:
  application:
    name: configuration-service
  cloud:
     config:
        server:
            git:
              uri: https://github.com/pradeepantal/test.git
          
server:
  port: 8888
  
eureka:
  client:
     registerWithEureka: false
     fetchRegistry: false
  instance:
     preferIpAddress: true
     